tunnel: DRIVER_LOGIN_TUNNEL_ID
credentials-file: /etc/cloudflared/DRIVER_LOGIN_TUNNEL_ID.json

ingress:
  - hostname: your-domain.example.com
    service: http://localhost:80
  - service: http_status:404
